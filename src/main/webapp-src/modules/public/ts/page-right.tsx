import * as React from 'react'
import * as ReactDom from 'react-dom'

import HotSchool from './hot-school'

import {Card} from 'antd'

export default class PageRight extends React.Component<any, any> {
    render() {
        return (
            <a />
        )
    }
}